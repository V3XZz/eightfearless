# Eight Fearless Website

This project contains a simple website structure for the "Eight Fearless" class site. The `images/` folder contains placeholder image files organized into `logos/`, `gallery/`, and `blog/`. The `assets/` folder contains `icons/` and `fonts/` directories. Replace the placeholder files with real images as needed.
